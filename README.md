In order to run on local phisical device:
    - Go Build Variants (lower-left corner) > Active Build Variant > change it back to Debug
    - IMPORTANT: change that data to release to live environment again

